# Bidirectional Client Demo

This project demonstrates how to connect to a duplex server and exchange Protobuf messages in both directions.

## How to Run

1. Ensure the Bidirectional Server is running.
2. Start the project:
   ```pwsh
   cd demos/ProtobuffEncoder.Demo.Bidirectional.Client
   dotnet run
   ```

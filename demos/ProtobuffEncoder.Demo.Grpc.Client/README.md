# gRPC Client Demo

This project demonstrates how to use the `ProtobuffEncoder.Grpc` library to communicate with a gRPC server.

## How to Run

1. Ensure the gRPC Server is running.
2. Start the project:
   ```pwsh
   cd demos/ProtobuffEncoder.Demo.Grpc.Client
   dotnet run
   ```

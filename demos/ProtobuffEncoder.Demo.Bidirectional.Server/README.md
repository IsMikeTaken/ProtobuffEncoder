# Bidirectional Server Demo

This project demonstrates how to host a duplex server that communicates with clients using Protobuf.

## How to Run

1. Start the project:
   ```pwsh
   cd demos/ProtobuffEncoder.Demo.Bidirectional.Server
   dotnet run
   ```

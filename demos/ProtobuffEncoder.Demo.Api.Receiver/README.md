# API Receiver Demo

This project demonstrates how to receive Protobuf messages via a simple HTTP API.

## How to Run

1. Start the project:
   ```pwsh
   cd demos/ProtobuffEncoder.Demo.Api.Receiver
   dotnet run
   ```
2. The server will listen for incoming Protobuf messages on its configured port.

# gRPC Server Demo

This project demonstrates how to host a gRPC server that uses Protobuf for its services.

## How to Run

1. Start the project:
   ```pwsh
   cd demos/ProtobuffEncoder.Demo.Grpc.Server
   dotnet run
   ```

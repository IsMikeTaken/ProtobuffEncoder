# API Sender Demo

This project demonstrates how to send Protobuf messages to a remote API.

## How to Run

1. Start the project:
   ```pwsh
   cd demos/ProtobuffEncoder.Demo.Api.Sender
   dotnet run
   ```
2. The sender will periodically send Protobuf messages to a configured URL.

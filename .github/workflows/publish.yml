# RETIRED — superseded by ci-release.yml (push to master).
# This file is kept as a no-op so existing branch protection rules referencing
# "Publish to NuGet" continue to resolve without error.
name: Publish to NuGet (retired)
on:
  workflow_dispatch:
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is retired. See ci-release.yml."

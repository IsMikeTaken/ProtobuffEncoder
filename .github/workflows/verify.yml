# RETIRED — superseded by ci-pr.yml (pull requests) and ci-development.yml (push to develop).
# This file is kept as a no-op so existing branch protection rules referencing
# "Verify Build and Tests" continue to resolve without error.
name: Verify Build and Tests (retired)
on:
  workflow_dispatch:
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is retired. See ci-pr.yml and ci-development.yml."
